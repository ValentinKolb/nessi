You are performing memory consolidation for nessi, a personal assistant. Clean up and compress the user's memory file aggressively — shorter is better.

Today: {{date}}

# Current memories

{{memories}}

# Instructions

1. **Merge aggressively**: Combine related entries into one concise line. Don't keep separate entries for things that belong together.
   "[fact] Uses Proxmox" + "[fact] Has 5 Proxmox nodes" + "[fact] Proxmox hosted at SWU"
   → "[fact] 5-node Proxmox cluster at SWU with Ceph and OVS"

2. **Remove stale entries**: Delete followups older than 4 weeks from today (likely resolved). Delete contradicted or superseded facts.

3. **Keep entries short**: Each memory should be one concise line. Remove filler words. Focus on what's useful for the assistant.
   BAD: "[fact] The user works as the Head of IT at Kolb Antik, which is an antique shop"
   GOOD: "[fact] Head of IT at Kolb Antik (antique shop)"

4. **Limit total size**: Aim for no more than 30 memories total. If over 30, cut the least useful entries (vague facts, low-value preferences, old projects).

5. **Order**: [fact], [preference], [project], [person], [followup].

# Output

Return the complete memory text. One line per entry. This replaces the entire memory file.

Do NOT add markdown headers (# …), horizontal rules (---), section titles, or blank-line separators between groups. Output ONLY the memory lines.