{
  "name": "nessi-monorepo",
  "description": "Minimal agent loop, provider adapters, and browser UI for nessi.",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  },
  "scripts": {
    "dev": "bun run --filter nessi-ui dev",
    "build": "bun run --filter nessi-ui build",
    "typecheck": "bun run --filter nessi-ai typecheck && bun run --filter nessi-core typecheck && bun run --filter nessi-ui typecheck",
    "test": "bun run --filter nessi-ai test && bun run --filter nessi-core test",
    "test:ai": "bun run --filter nessi-ai test",
    "test:core": "bun run --filter nessi-core test",
    "dev:ui": "bun run --filter nessi-ui dev",
    "build:ui": "bun run --filter nessi-ui build",
    "preview:ui": "bun run --filter nessi-ui preview"
  }
}
